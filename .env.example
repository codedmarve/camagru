DB_HOST=db
DB_NAME=camagru
DB_USER=camagru_user
DB_PASS=change_me
DB_ROOT_PASS=change_me_root

# Mail — dev defaults use Mailhog (no TLS/auth).
# For real email, point these at an SMTP relay, e.g. Gmail:
#   MAIL_HOST=smtp.gmail.com  MAIL_PORT=587  MAIL_TLS=on  MAIL_AUTH=on
#   MAIL_USER=you@gmail.com   MAIL_PASS=<app password>  MAIL_FROM=you@gmail.com
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_FROM=noreply@camagru.local
MAIL_TLS=off
MAIL_AUTH=off
MAIL_USER=
MAIL_PASS=
